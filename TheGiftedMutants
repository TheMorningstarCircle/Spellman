local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("The Gifted Mutants GUI Made By ɹɐʇsɓuᴉuɹoW ɐuᴉɹqɐS", "BloodTheme")

-- Characters
local Tab = Window:NewTab("Characters")
local Section = Tab:NewSection("Main")

Section:NewButton("Agatha Harkness", "", function()
   -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "AgathaHarkness"
}

game:GetService("ReplicatedStorage").Events.DeployCharacter:FireServer(unpack(args))
end)

Section:NewButton("Wiccan", "", function()
 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Wiccan"
}

game:GetService("ReplicatedStorage").Events.DeployCharacter:FireServer(unpack(args))
end)

Section:NewButton("Jean Grey Dark Phoenix", "", function()
 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Jean Grey: Dark Phoenix"
}

game:GetService("ReplicatedStorage").Events.DeployCharacter:FireServer(unpack(args))
end)

Section:NewButton("838 Wanda", "", function()
   -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Wanda Maximoff: Earth 838"
}

game:GetService("ReplicatedStorage").Events.DeployCharacter:FireServer(unpack(args))
end)

Section:NewButton("Speed", "", function()
  -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Speed"
}

game:GetService("ReplicatedStorage").Events.DeployCharacter:FireServer(unpack(args))
end)

local Tab = Window:NewTab("Powers")
local Section = Tab:NewSection("Agatha Harkness Powers")

Section:NewToggle("Siphon", "Keybind (1)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "1" then
            local player = getPlayer()
            if player then

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Siphon",
    [2] = player.Character
}

game:GetService("ReplicatedStorage").Events.AgathaEvent:FireServer(unpack(args))
end
end
end)
end)

Section:NewButton("Shield On", "", function()
   -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Shield",
    [2] = "Begin"
}

game:GetService("ReplicatedStorage").Events.AgathaEvent:FireServer(unpack(args))
end)

Section:NewButton("Shield Off", "", function()
 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Shield",
    [2] = "End"
}

game:GetService("ReplicatedStorage").Events.AgathaEvent:FireServer(unpack(args))
end)

Section:NewButton("Runes", "", function()
 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Runes",
    [2] = "Begin"
}

game:GetService("ReplicatedStorage").Events.AgathaEvent:FireServer(unpack(args))
end)

Section:NewButton("Burst", "", function()
  -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Burst"
}

game:GetService("ReplicatedStorage").Events.AgathaEvent:FireServer(unpack(args))
end)

local Section = Tab:NewSection("Wiccan Powers")

Section:NewButton("Shield On", "", function()
 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Shield",
    [2] = "Begin"
}

game:GetService("ReplicatedStorage").Events.WiccanEvent:FireServer(unpack(args))
end)
 
 Section:NewButton("Shield Off", "", function()
  -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Shield",
    [2] = "End"
}

game:GetService("ReplicatedStorage").Events.WiccanEvent:FireServer(unpack(args))
end)

Section:NewButton("Burst", "", function()
   -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Burst"
}

game:GetService("ReplicatedStorage").Events.WiccanEvent:FireServer(unpack(args))
end)

Section:NewButton("Heal", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Heal"
}

game:GetService("ReplicatedStorage").Events.WiccanEvent:FireServer(unpack(args))
end)
 

local Section = Tab:NewSection("Jean Grey: Dark Phoenix Powers")

Section:NewToggle("Disintegration", "Keybind (2) Not Ingame Yet", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "2" then
            local player = getPlayer()
            if player then

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Disintegrate",
    [2] = player.Character
}

game:GetService("ReplicatedStorage").Events.DarkPhoenixEvent:FireServer(unpack(args))

end
end
end)
end)

Section:NewToggle("Crack", "Keybind (3) Not Ingame Yet", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "3" then
            local player = getPlayer()
            if player then

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Crack",
    [2] = player.Character
}

game:GetService("ReplicatedStorage").Events.DarkPhoenixEvent:FireServer(unpack(args))

end
end
end)
end)

Section:NewToggle("Siphon", "Keybind (4)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "4" then
            local player = getPlayer()
            if player then

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Siphon",
    [2] = player.Character
}

game:GetService("ReplicatedStorage").Events.DarkPhoenixEvent:FireServer(unpack(args))


end
end
end)
end)

Section:NewButton("Shield On", "", function()
 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Shield",
    [2] = "Begin"
}

game:GetService("ReplicatedStorage").Events.DarkPhoenixEvent:FireServer(unpack(args))
end)

Section:NewButton("Shield Off", "", function()
  -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Shield",
    [2] = "End"
}

game:GetService("ReplicatedStorage").Events.DarkPhoenixEvent:FireServer(unpack(args))
end)

Section:NewButton("Burst", "", function()
  -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Burst",
    [2] = Vector3.new(219.88970947265625, 5, -232.7559356689453),
    [3] = 5.73721969127655
}

game:GetService("ReplicatedStorage").Events.DarkPhoenixEvent:FireServer(unpack(args))
end)

Section:NewButton("Ultimate", "", function()
  -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Ultimate",
    [2] = "Begin"
}

game:GetService("ReplicatedStorage").Events.DarkPhoenixEvent:FireServer(unpack(args))
end)

local Section = Tab:NewSection("838 Wanda Powers")

Section:NewButton("Shield On", "", function()
   -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Shield",
    [2] = "Begin"
}

game:GetService("ReplicatedStorage").Events:FindFirstChild("838Event"):FireServer(unpack(args))

   end)
   
   Section:NewButton("Shield Off", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Shield",
    [2] = "End"
}

game:GetService("ReplicatedStorage").Events:FindFirstChild("838Event"):FireServer(unpack(args))
end)

Section:NewButton("Heal", "", function()
   -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Heal"
}

game:GetService("ReplicatedStorage").Events:FindFirstChild("838Event"):FireServer(unpack(args))
end)

local Section = Tab:NewSection("Speed Powers")

Section:NewButton("Time Stop", "", function()
 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Time Stop"
}

game:GetService("ReplicatedStorage").Events.SpeedEvent:FireServer(unpack(args))
end)
   
Section:NewButton("Tornado Creation", "", function()
 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "Tornado Creation"
}

game:GetService("ReplicatedStorage").Events.SpeedEvent:FireServer(unpack(args))
end)

local Tab = Window:NewTab("Others")
local Section = Tab:NewSection("Misc")

Section:NewButton("Infinite Yield", "Admin", function()
    loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
end)

Section:NewButton("Btools", "", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/TheMorningstarCircle/Spellman/main/Btools"))()
end)

Section:NewButton("CMD Admin", "", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/TheMorningstarCircle/Spellman/main/CMD"))()
end)

Section:NewButton("Dark Dex", "Dex", function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/TheMorningstarCircle/Spellman/main/Dex"))()
end)

Section:NewButton("Simple Spy", "Remote Spy", function()
    loadstring(game:HttpGet("https://github.com/exxtremestuffs/SimpleSpySource/raw/master/SimpleSpy.lua"))()
end)

Section:NewButton("Hydroxide", "Remote Spy", function()
    loadstring(game:HttpGetAsync('https://raw.githubusercontent.com/TheMorningstarCircle/Spellman/main/Hydroxide', true))()
end)

Section:NewButton("In-Line Translator", "Translate", function()
    loadstring(game:HttpGetAsync('https://i.qts.life/r/ChatInlineTranslator.lua', true))()
end)

local Tab = Window:NewTab("Credits")
local Section = Tab:NewSection("Created by ɹɐʇsɓuᴉuɹoW ɐuᴉɹqɐS")

local Section = Tab:NewSection("Toggle UI")
Section:NewKeybind("Toggle UI", "Toggle UI", Enum.KeyCode.Zero, function()
	Library:ToggleUI()
end)

wait(1)
game.StarterGui:SetCore("SendNotification", { 
    Title = "★ᵀʰᵉ ᴹᵒʳⁿⁱⁿᵍˢᵗᵃʳ ᶜⁱʳᶜˡᵉ★";
    Text = "The Gifted Mutants    ~~~~~~ Now Active ~~~~~~";               
    Icon = "rbxthumb://type=Asset&id=10845688213&w=150&h=150";
    Duration = "60";
    Button1 = "Close";
})
