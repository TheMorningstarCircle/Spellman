local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/xHeptc/Kavo-UI-Library/main/source.lua"))()
local Window = Library.CreateLib("CAOS: Between Light And Darkness Made By ɹɐʇsɓuᴉuɹoW ɐuᴉɹqɐS", "BloodTheme")

    -- Characters
local Tab = Window:NewTab("Characters")

local Section = Tab:NewSection("Witches")

Section:NewButton("Sabrina Morningstar", "", function()
   -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "SabrinaMorningstar"
}

game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
end)

Section:NewButton("Sabrina Spellman", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "SabrinaSpellman"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Zelda Spellman", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "ZeldaSpellman"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Hilda Spellman", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "HildaSpellman"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Ambrose Spellman", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "AmbroseSpellman"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Prudence Blackwood", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "PrudenceBlackwood"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Agatha Night", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "Agatha"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Dorcas Night", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "Dorcas"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Father Blackwood", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "FatherBlackwood"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Nicholas Scratch", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "NicholasScratch"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

Section:NewButton("Caliban", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "Caliban"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Lilith", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "Lilith"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

Section:NewButton("Lucifer Morningstar", "", function()
   -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = "LuciferMorningstar"
}

game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
end)

 Section:NewButton("Cheryl Blossom", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "CherylBlossom"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 local Section = Tab:NewSection("Pagan Witches")

 Section:NewButton("Circe", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "Circe"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Nagaina", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "Nagaina"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Pan", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "Pan"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

local Section = Tab:NewSection("Mortals")

Section:NewButton("Harvey Kinkle", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "HarveyKinkle"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Rosalind Walker", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "RosalindWalker"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 Section:NewButton("Theo Putnam", "", function()
    -- Script generated by SimpleSpy - credits to exx#9394
 
 local args = {
     [1] = "TheoPutnam"
 }
 
 game:GetService("ReplicatedStorage").Remotes.DeployCharacter:FireServer(unpack(args))
 end)

 local Tab = Window:NewTab("Abilities")
 local Section = Tab:NewSection("Works on Everyone")

 local Section = Tab:NewSection("Keybinds")

 Section:NewToggle("Ignis", "Keybind (1)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "1" then
            local player = getPlayer()
            if player then

                
-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitIgnis:FireServer(unpack(args))



end
end
end)
end)

Section:NewToggle("Sleep", "Keybind (2)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "2" then
            local player = getPlayer()
            if player then

                
-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitDeepSleep:FireServer(unpack(args))




end
end
end)
end)

Section:NewToggle("Heal", "Keybind (3)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "3" then
            local player = getPlayer()
            if player then

                
-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitHeal:FireServer(unpack(args))




end
end
end)
end)

Section:NewToggle("Profugio", "Keybind (4)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "4" then
            local player = getPlayer()
            if player then

                
-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitProfugio:FireServer(unpack(args))

-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitProfugio:FireServer(unpack(args))

-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitProfugio:FireServer(unpack(args))

-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitProfugio:FireServer(unpack(args))

-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitProfugio:FireServer(unpack(args))

end
end
end)
end)

Section:NewToggle("Prohibere", "Keybind (5)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "5" then
            local player = getPlayer()
            if player then

                
-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitProhibere:FireServer(unpack(args))

end
end
end)
end)

Section:NewToggle("Choke", "Keybind (6)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "6" then
            local player = getPlayer()
            if player then

                
-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitChoke:FireServer(unpack(args))

end
end
end)
end)

Section:NewToggle("Telekenetic Push", "Keybind (7)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "7" then
            local player = getPlayer()
            if player then

                
-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitTelekeneticPush:FireServer(unpack(args))

end
end
end)
end)

Section:NewToggle("Pain Infliction", "Keybind (8)", function()

    local function getPlayer()
        local userInput = game:GetService("UserInputService")
        local cam = workspace.CurrentCamera
        local mousePos = userInput:GetMouseLocation()
        local mouseRay = cam:ViewportPointToRay(mousePos.X, mousePos.Y)
        players = {}
        for _, x in next, game:GetService('Players'):GetPlayers() do
            players[#players + 1] = x.Character
        end
        local raycastParams = RaycastParams.new()
        raycastParams.FilterType = Enum.RaycastFilterType.Whitelist
        raycastParams.FilterDescendantsInstances = players
        raycastParams.IgnoreWater = true
        local raycastResult = workspace:Raycast(mouseRay.Origin, mouseRay.Direction * 1000, raycastParams)
        if raycastResult then
            for _, x in next, game:GetService('Players'):GetPlayers() do
                if not raycastResult.Instance:IsDescendantOf(x.Character) then
                    continue
                end
                return x
            end
        end
        return false
    end

    local function Set(list)
        local set = {}
        for _, l in ipairs(list) do
            if l then
                set[l] = true
            end
        end
        return set
    end

    game.Players.LocalPlayer:GetMouse().KeyDown:Connect(function(key)
        if key == "8" then
            local player = getPlayer()
            if player then

                
-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitPainInfliction:FireServer(unpack(args))

end
end
end)
end)

local Section = Tab:NewSection("Works on Specific Character")
local Section = Tab:NewSection("Sabrina Morningstar / Sabrina Spellman")

Section:NewTextBox("Hellfire", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitHellfire:FireServer(unpack(args))
end)

Section:NewTextBox("Kneel", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

  -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312),
        ["hit"] = player.Character.Head.Headz
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitKneel:FireServer(unpack(args))
end)

Section:NewTextBox("Hell Banish", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitHellBanish:FireServer(unpack(args))
end)

Section:NewTextBox("Hell Sending", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitHellSending:FireServer(unpack(args))

end)

Section:NewTextBox("Immensus Dolor", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitImmensusDolor:FireServer(unpack(args))

end)

Section:NewTextBox("Void Consume", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitVoidConsume:FireServer(unpack(args))
end)

local Section = Tab:NewSection("Lucifer")

Section:NewTextBox("Disintegration", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitDisintegration:FireServer(unpack(args))
end)

Section:NewTextBox("Hell Banish", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitHellBanish:FireServer(unpack(args))
end)

local Section = Tab:NewSection("Lilith")

Section:NewTextBox("Lilith Scream", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitLilithScream:FireServer(unpack(args))

-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitLilithScream:FireServer(unpack(args))

-- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitLilithScream:FireServer(unpack(args))
end)

local Section = Tab:NewSection("Circe")

Section:NewTextBox("Spider Transformation", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitSpiderTransformation:FireServer(unpack(args))
end)

local Section = Tab:NewSection("Cheryl")

Section:NewTextBox("Cheryl Ignis", "", function(txt)
    local Players = game:GetService("Players")
    local msg = txt
    local function findPlayer(stringg)
        for _, v in pairs(Players:GetPlayers()) do
            if stringg:lower() == (v.Name:lower()):sub(1, #stringg) then
                return v
            end
        end
    end

    local player = findPlayer(msg)
    local CHAR = player.Character

 -- Script generated by SimpleSpy - credits to exx#9394

local args = {
    [1] = {
        ["hit"] = player.Character.Head.Headz,
        ["position"] = Vector3.new(188.3291778564453, 7.194432258605957, 108.26394653320312)
    }
}

game:GetService("ReplicatedStorage").Remotes.InitSpell.InitCherylIgnis:FireServer(unpack(args))
end)

local Tab = Window:NewTab("Others")
local Section = Tab:NewSection("Misc")
                
                Section:NewButton("Infinite Yield", "Admin", function()
                    loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
                end)
                
                Section:NewButton("Btools", "", function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/TheMorningstarCircle/Spellman/main/Btools"))()
                end)
                
                Section:NewButton("CMD Admin", "", function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/TheMorningstarCircle/Spellman/main/CMD"))()
                end)
                
                Section:NewButton("Dark Dex", "Dex", function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/TheMorningstarCircle/Spellman/main/Dex"))()
                end)
                
                Section:NewButton("Simple Spy", "Remote Spy", function()
                    loadstring(game:HttpGet("https://github.com/exxtremestuffs/SimpleSpySource/raw/master/SimpleSpy.lua"))()
                end)
                
                Section:NewButton("Hydroxide", "Remote Spy", function()
                    loadstring(game:HttpGetAsync('https://raw.githubusercontent.com/TheMorningstarCircle/Spellman/main/Hydroxide', true))()
                end)
                
                Section:NewButton("In-Line Translator", "Translate", function()
                    loadstring(game:HttpGetAsync('https://i.qts.life/r/ChatInlineTranslator.lua', true))()
                end)
                
                local Tab = Window:NewTab("Credits")
                local Section = Tab:NewSection("Created by ɹɐʇsɓuᴉuɹoW ɐuᴉɹqɐS")
                
                local Section = Tab:NewSection("Toggle UI")
                Section:NewKeybind("Toggle UI", "Toggle UI", Enum.KeyCode.Zero, function()
                    Library:ToggleUI()
                end)
                
                wait(1)
                game.StarterGui:SetCore("SendNotification", { 
                    Title = "★ᵀʰᵉ ᴹᵒʳⁿⁱⁿᵍˢᵗᵃʳ ᶜⁱʳᶜˡᵉ★";
                    Text = "CAOS GUI    ~~~~~~ Now Active ~~~~~~";               
                    Icon = "rbxthumb://type=Asset&id=10845688213&w=150&h=150";
                    Duration = "60";
                    Button1 = "Close";
                })
